Задача: напиши Python-скрипт top_words.py. Он принимает путь к текстовому файлу как аргумент командной строки и печатает топ-5 самых частых слов. Счёт идёт без учёта регистра, пунктуация (пробелы, дефисы, кавычки) игнорируется. Формат вывода: 'слово: количество', одно слово на строку, по убыванию частотности; при равных — по алфавиту. Пустой файл — ничего не печатать. Выведи ТОЛЬКО код скрипта, без объяснений и markdown.
