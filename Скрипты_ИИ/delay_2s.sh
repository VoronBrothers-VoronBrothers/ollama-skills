#!/usr/bin/env bash
# delay_2s.sh — имитация задержки: ждёт 2 секунды и выходит (exit 0)
# опционально: ./delay_2s.sh 5 — ждать 5 секунд (число в аргументе)
n="${1:-2}"
sleep "$n"
echo "delay_${n}s: прошло ${n} с"
