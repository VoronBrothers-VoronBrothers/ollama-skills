## Почему стоит использовать `instructor`?

### Проблема: ненадёжные неструктурированные ответы от LLM

Без `instructor` вы получаете **текст**, а не структурированные данные:

```python
# Что вы хотите:
user_info = extract_user("John is 25 years old")
print(user_info.name)  # "John"
print(user_info.age)   # 25

# Что вы получаете на практике:
response = llm.complete("Extract: John is 25 years old")
# "I'd be happy to help! Based on the text, the user's name is John
# and their age is 25. Is there anything else you'd like me to extract?"

# Теперь нужно:
# 1. Парсить текст вручную
# 2. Обрабатывать синтаксические ошибки JSON
# 3. Валидировать данные
# 4. Повторять запрос при ошибках (а они будут)
# 5. Писать отдельный код под каждый API провайдера
```

### Решение: `instructor` + Pydantic

```python
import instructor
from pydantic import BaseModel

class User(BaseModel):
name: str
age: int

client = instructor.from_provider("openai/gpt-4o-mini")
user = client.create(
response_model=User,
messages=[{"role": "user", "content": "John is 25 years old"}],
)

print(user.name)  # "John"
print(user.age)   # 25
```

✅ Без парсинга
✅ Без ручных retry-логик
✅ Без провайдерозависимого кода

---

## Ключевые задачи, которые решает `instructor`

### 1. Надёжность: автоматические повторы при валидации

```python
# Без instructor — хрупкий код:
try:
data = json.loads(llm_response)
user = User(**data)  # KeyError: 'name'
except Exception as e:
# Что делать? Retry? Парсить текст?
pass

# С instructor — надёжно:
user = client.create(
response_model=User,
messages=[{"role": "user", "content": "..."}],
max_retries=3,  # Автоматические повторы при ошибках валидации
)
# Возвращает валидный `User` или выбрасывает исключение
```

### 2. Унификация API: один интерфейс для всех провайдеров

```python
# OpenAI:
client = instructor.from_provider("openai/gpt-4o-mini")

# Anthropic:
client = instructor.from_provider("anthropic/claude-3")

# Google:
client = instructor.from_provider("google/gemini-pro")

# Одинаковый вызов:
user = client.create(
response_model=User,
messages=[{"role": "user", "content": "..."}],
)
```

### 3. Сложные структуры данных — как в Python

```python
from typing import List

class Address(BaseModel):
street: str
city: str

class User(BaseModel):
name: str
addresses: List[Address]

class UserList(BaseModel):
users: List[User]

result = client.create(
response_model=UserList,
messages=[{"role": "user", "content": "..."}],
)
```

---

## Реальные последствия без `instructor`

| Категория | Потери |
|----------|--------|
| **Время** | 2–3 ч на парсинг/валидацию<br>4–6 ч на отладку edge cases<br>2–3 ч на интеграцию нового провайдера |
| **Баги в продакшене** | Crashes из-за malformed JSON<br>Скрытые ошибки при отсутствии полей<br>Несоответствие
типов → повреждение БД |
| **Разработчики** | «Работало же в тестах!»<br>«Почему JSON другой?»<br>«Как обрабатывать строку вместо числа?» |

---

## Отзывы разработчиков

- ✅ *«Сократили LLM-код на 80%»*
- ✅ *«Наконец-то могу доверять ответам LLM»*
- ✅ *«Retry-логика уже окупила установку»*
- ✅ *«Работает одинаково у всех провайдеров — больше нет vendor lock-in»*

---

## Когда `instructor` не нужен?

- Вам нужны **сырые текстовые ответы** (чат-боты, генерация текста)
- Вы пишете одноразовый скрипт без обработки ошибок
- Вам нравится отлаживать JSON в 3 ночи

---

## Начало работы (30 секунд)

```bash
pip install instructor
```

```python
import instructor
from pydantic import BaseModel

client = instructor.from_provider("openai/gpt-4o-mini")

class User(BaseModel):
name: str
age: int

user = client.create(
response_model=User,
messages=[{"role": "user", "content": "John is 25 years old"}],
)

print(user)  # User(name='John', age=25)
```

---

[Ключевые слова]: instructor, LLM, pydantic, structured output, JSON validation, automatic retries, provider
abstraction, OpenAI, Anthropic, Google Gemini, API unification, data modeling, error handling, type safety
