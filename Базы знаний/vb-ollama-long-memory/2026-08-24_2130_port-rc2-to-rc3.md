# Ollama: порт кастомных правок rc2 → rc3 (0.33.0)

Дата/время: 2026-08-25 ~21:30

## Что сделано
В каталог `ollama 0.33.0-rc3` (Git: "GitHub ollama") скопированы локальные правки из `ollama 0.33.0-rc2`. До копирования подтверждено diff'ом, что разница — только наши патчи (upstream-расхождений нет).

Скопированные файлы (4):
- `agent/tools/file.go` — normalizeToolPath: `~` в home, absolute/relative пути от cwd (устраняет «absolute paths are not allowed» / «path escapes working directory»).
- `agent/tools/file_test.go`, `agent/tools/file_unix_test.go` — тесты под новую логику (TestEditRejectsEscapingPath и т.п.).
- `cmd/agent_tui.go` — без английской boilerplate в system-prompt, low thinking по умолчанию.

## Проверка
- `go build ./cmd/... ./agent/...` → EXIT=0.
- `go test ./agent/tools/ -count=1` → ok.

## Состояние
rc3 теперь содержит те же кастомные правки, что rc2. Binaru для продаю еще не собран из rc3; когда будет нужен билд, собрать из rc3 (команды из Инструкции_инструменты_пересборка_оллама.md).

## Откат
`git checkout -- agent/tools/file.go agent/tools/file_test.go agent/tools/file_unix_test.go cmd/agent_tui.go` в rc3 + rebuild.
