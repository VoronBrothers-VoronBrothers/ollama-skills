# Ollama 0.33.0-rc2: пересобранный бинарник, режим low по умолчанию

Дата/время: 2026-08-24 ~06:30

## Что важно запомнить
1. **На текущей версии ollama (0.33.0-rc2) бинарник пересобран** — это
   кастомная сборка, а не стоковая дистрибутивная версия. Обновлять/переустанавливать
   ollama штатными способами нельзя без потери кастомных изменений: сначала сохранить
   исходники/наработки (пример сохранения: file.go.orig-20260824 в этой папке).
2. **По умолчанию режим обдумывания — low.** В TUI при старте low thinking
   подхватывается автоматически.

## Следствие (уже выполнено 2026-08-24)
В Скрипты_ИИ/ollama_console.sh удалён блок «/think → Enter → Down → Down → Enter»
(шаги бывших 9–13): выбирать low клавиатурной комбинацией больше не нужно.
Если в какой-то сценарии low вдруг перестанет применяться по умолчанию — вербальный
признак: модель думает много/долго без /think, либо TUI стартует в другом режиме;
восстановить блок можно из истории этого скрипта (bak: ollama_console.sh.bak).

## Проверка версии
    ollama --version   # 0.33.0-rc2 (проверено 2026-08-24 06:25)

## Пути в edit/read (то же rebuild v0.33.0-rc2)
v0.33.0-rc2: normalizeToolPath — полный набор путей: `~` раскрывается в home,
`../` за cwd работает, абсолютные и относительный от cwd. Старые ошибки
«absolute paths are not allowed» и «path escapes working directory» СНЯТЫ.
При откате бинарника на стоковую версию это ПОТЕРЯЕТСЯ: read/edit снова не примут
`~` и пути за cwd (а также режим low по умолчанию — см. выше).
E2E-проверка 24.08 (~07:55): read `~/.zshrc` success; edit `../e2e_e2e_escape.txt`
за cwd success (реально записано, проверено на диске); read nonexistent → no such file.
