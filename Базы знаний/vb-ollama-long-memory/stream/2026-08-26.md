
## 04:35
Привет, это тебе от тебя же (26.08.2026). Всё в порядке.

## 05:30
2026-08-26 (Konsole/ollama TUI) — Queue poller DONE. Оллма-репо: /home/voron/Документы/VB-ollama/GitHub ollama/ollama 0.33.0-rc3. Фича: если run падает с сетевой ошибкой (connection refused / unexpected EOF) при непустой pendingPrompts — не дренируем очередь, а ждём демона. Реализация: cmd/tui/chat/events.go + daemonPingMsg{err error}; cmd/tui/chat/chat.go + поле pollingDaemon bool, case daemonPingMsg (ошибка→повтор ping; успех→startRun(pendingPrompts[0])), func daemonPingCmd() (api.ClientFromEnvironment → c.List(ctx) = GET /api/tags, timeout 2s), func isNetworkDownError() (net.Error / "connection refused" / "unexpected EOF", не cancel). go build/vet OK. Патч всех локальных изменений (вкл. раннюю очередь + aliases): /home/voron/Документы/VB-ollama/patch_queue-poller.patch (git diff, 1031 строка, 4 файла: chat.go events.go input.go render.go). Не тестировано вживую (kill ollama → 2 сообщения → waiting → авто-рестарт очереди).

## 05:42
2026-08-26: очередь — пометка для модели. При dequeue (оба места в cmd/tui/chat/chat.go: chatRunDoneMsg и daemonPingMsg) next оборачивается queuedMessagePrefix() из input.go: "[Сообщение поставлено в очередь и сейчас доставлено как следующее]\n" + текст. Префикс добавляется при отправке (не при enqueue), поэтому UI-отображение очереди (render.go) чистое, без мусора. Пересобрано: 0.33.0-rc3-voron, /usr/local/bin/ollama.

## 05:55
## Добавка от 26.08: пометка очередных сообщений для модели
При dequeue (оба места в cmd/tui/chat/chat.go) next оборачивается queuedMessagePrefix() (input.go): "[Сообщение поставлено в очередь и сейчас доставлено как следующее]\n" + текст. Префикс при отправке, не при enqueue — UI-статус очереди чистый. Собрал 0.33.0-rc3-voron.

## 05:57
Дополнение: префикс сократили до "[из очереди]\n" (бывший длинный съедал токены). Итог 26.08: queuedMessagePrefix() = "[из очереди]\n" + текст.

## 06:39
26.08.2026: Поставил tmux (apt). Умею: new-session -d -s имя → send-keys -t имя "cmd" Enter → capture-pane -t имя -p (читать экран) → kill-session. Для интерактивных CLI/repl, долгих процессов. Важно: после send-keys ставить паузу (sleep), иначе гонка при старте программы. Источник идей: /home/voron/Документы/VB-ollama/GitHub ollama and others/openclaw/ (скиллы video-frames, summarize, tmux).

## 06:54
Запомненное число: 42 (26.08.2026)

## 07:17
Живая tmux-сессия `py`: python3 REPL, открыт в ~. Не закрывать, пока пользователь не попросит.

## 07:18
Сессия py: python3 завершён (exit), в сессии чистый zsh, она открыта.

## 07:18
tmux py: python3 завершён, чистый zsh, сессия открыта (не закрывать).

## 07:18
tmux py: python3 завершён, чистый zsh, сессия открыта (не закрывать).

## 07:18
tmux py: python3 завершён, чистый zsh, сессия открыта (не закрывать).

## 07:20
Урок: завершать сессию tmux надёжнее `tmux kill-session -t имя`, не `exit` — exit в последнем окне роняет весь tmux-сервер (все сессии).

## 16:37
2026-08-26: Оллала TUI — время рядом с сообщениями. Изменения в /home/voron/Документы/VB-ollama/GitHub ollama and others/ollama 0.33.0-rc3: (1) cmd/tui/chat/chat.go — user-entry при добавлении получает startedAt: time.Now(). (2) cmd/tui/chat/render.go — assistant entry в ensureAssistantEntry тоже со startedAt; новая func entryTimestampLine(entry) рендерит chatMetaStyle.Render(entry.startedAt.Format("15:04:05")); в renderEntryLines для case "assistant" и case "user" до lines дописывается ts через append([]string{ts}, lines...). Сборка: cd каталог; go build -o /tmp/ollama-ts . (проверить: tmux new-session + запуск бинарника, чат — над user и assistant сообщениями появляется время). go vet ./cmd/tui/chat/ — чисто.

## 16:39
2026-08-26: Заменил текущий бинарник ollama (/usr/local/bin/ollama) на пересобранный с времен stamp. Сборка с версией: go build -ldflags "-X github.com/ollama/ollama/version.Version=0.33.0-rc3-voron" -o /tmp/ollama-ts . (без ldflags версия 0.0.0). Замена: cp ругается "Текстовый файл занят" (процесс держит бинарник) — использовать sudo cp file /usr/local/bin/ollama.new && sudo mv -f /usr/local/bin/ollama.new /usr/local/bin/ollama && sudo chown voron:voron. sudo без пароля работает.

## 17:30
Клон оркестратора (ollama run orchestrator:latest в tmux) = я сам в чистом REPL без TUI-меню. Плюс: модель уже загружена в RAM/VRAM, нет ожидания загрузки — скрипты зрения (зрение_хром.sh и др.) по умолчанию вызывают orchestrator:latest именно ради скорости. granite3.2-vision:2b слаба для точных задач (ответила "толкой" на запрос координат).

## 18:12
Правило по tmux-сессии zrenie_run: если используется модель по умолчанию (orchestrator:latest), ждать ответа не дольше sleep 20 — опрашивать capture-pane чаще. Двойные клики НЕ использовать, только одиночные.

## 18:23
Инструмент прикрепления картинок (нашёл 26.08.2026 в репо ollama): TUI сам извлекает пути к .png/.jpg из текста сообщения (filedata.ExtractWithFiles, cmd/internal/filedata/filedata.go) и шлёт как Image #N. Чтобы увидеть картинку: написать путь к PNG прямо в сообщении (самовизыв). MultiModal авто-включается по CapabilityVision модели.

## 19:16
Зрение-координаты: точный путь — TUI в tmux: `ollama` → Enter (первый пункт) → сообщение "полный путь к PNG + задача". Так модель точно находит координаты кнопок. Другие способы отправки (скрипт zrenie_всё_run.sh, REPL ollama run напрямую) дают координаты мимо/косо. Правило: задачи поиска координат — только через TUI-запуск.

## 21:51
Правила kiosk-рецепта (Google/DeepSeek) от 26.08: 1) курсор уже в поле ввода — не кликать туда лишний раз; 2) в Google не жать Enter после текста, а кликнуть кнопку «Режим ИИ» (лупа с бликом); 3) если есть кнопка развернуть результат — нажать её; 4) если есть строка меню «Режим ИИ Все Покупки Картинки...» — нажать «Режим ИИ»; 5) если режим ИИ открыт в этом же поле — пролистать вниз и нажать кнопку копирования (самая нижняя левая, два прямоугольника); 6) ВКОНЦЕ обязательно выйти из кинка: alt+F4 или pkill chrome — иначе пользователь не знает, что скрипт закончен.

## 21:51
Корректировка 26.08: 6 пунктов правил (курсор уже в поле, не жать Enter а кликать лупу-ИИ, кнопка развернуть, строка меню Режим ИИ/Все/Покупки, пролистать и скопировать, выход из кинка) относятся ТОЛЬКО к Google (поиск ИИ), НЕ к DeepSeek. DeepSeek — свои правила: курсор тоже уже в поле ввода, но дальше по-своему.

## 22:11
2026-08-26 (вечер) — x11-agent: доводка и тест DeepSeek. Сделано: 1) Скилл перечитал с правками пользователя (правила Google «если есть» + цель «передать результат»; правило 8 — показывать завершённость; FAILSAFE → правый нижний угол). 2) В скрипте был стандартный pyautogui.FAILSAFE = ЛЕВЫЙ верхний угол — не совпадало со скиллом. Переписал: свой _failsafe_check() ловит мышь в пределах 15 px от правого нижнего угла, вызывает sys.exit(0). Проверен: перед каждым шагом и перед каждым действием. Тест (1918,1078) → стоп сработал. SKILL.md поправил: «Проверено 2026-08-26». 3) Ошибка при тесте DeepSeek: модель вместо вопроса ввела сам код-ответ (многострочный, с \n), xdotool шлёт \n как Enter → в поле DeepSeek сообщение улетело тремя отправлениями. Фикс: в _type_text() \n → пробел до ввода (защита от разбивки). 4) Ошибка формулировки задачи: «Напечатай: напиши код пайтона» — модель прочла как задание СЕБЕ и ответила сама. Правило: текст вопроса формулировать явно — «Напечатай ТОЧНО этот текст в кавычках: "..." и нажми Enter». 5) Тёмная сторона tmux: длинная команда (~300 символов) переносится в окне ~180 столбцов — визуально «разделилась», но bash получает её одной строкой; Enter один. Это только косметика. Ошибки по пути: pkill/exit code -1 в цепочках команд (не критично); pgrep chrome путает pid (процессы дочерние). Правило: перед каждым запуском агента pkill -f x11_agent.py + tmux kill-session.
