
## 07:21
Ollama agent skills: в YAML front matter (SKILL.md) description с двоеточием ОБЯЗАТЕЛЬНО брать в кавычки, иначе warning "mapping values are not allowed". Скиллы лежат в ~/.ollama/skills/<name>/SKILL.md.

## 16:30
Меню `ollama`: в корне оставлен только пункт «Chat, Code, & Work». Правки в cmd/tui/tui.go (buildMenuItems → только runModelMenuItem) + переписан tui_test.go; тесты PASS, пересборка с ldflags version 0.33.0-rc3-voron, sudo mv в /usr/local/bin/ollama. Ctrl+U очищает строку ввода в bubbletea-TUI через tmux send-keys C-u; Backspace send-keys не работает (уходит текстом). Всё записано в 2026-08-27_menu-run-only-ctrl-u.md, скилл tmux обновлён.
