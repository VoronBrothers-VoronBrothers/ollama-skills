# Rebase на v0.33.2-rc0 (v0.33.1+) — 27.08.2026, ~19:20

## Что сделано
1. **Сравнение.** Наша база (f6c59d87 = upstream main на момент rc3) vs v0.33.1 (тег 13f2fb8c) / v0.33.2-rc0 (39f7f915, это было в папке «ollama v0.33.1»). Upstream +19 коммитов: llama.cpp b10630, MLX bump + Qwen3.8 Flash Next, x/models/qwen4_exp (~2000 строк), фиксы proxy Claude Desktop, Pi OLLAMA_HOST, cmake/linux-фиксы. Пересечение изменённых файлов с нашими = 0 → конфликтов нет.
2. **Бинарники.** Сохранил копию текущего `/usr/local/bin/ollama` как `/usr/local/bin/ollama-2026-08-27_19-13`. Удалил мусор: `ollama-queue`, `ollama-symlinks`, `ollama.orig`, 4×`ollama.bak-*` (нужен sudo, файлы root). Из папки репо удалил старые ELF: `ollama-bin`, `ollama-queue`, `selfsay`.
3. **Коммит + rebase.** Закоммитил все dirty-файлы (17 файлов: TUI chat/input/render, cmd, agent/tools, selfsay, keytest, PLAN-xlow.md) → коммит `60b3cc8f`. Rebase на origin/main (39f7f915 = v0.33.2-rc0) — чисто.
4. **Пересборка.** Собрал версию `0.33.2-rc0-voron`, поставил mv в `/usr/local/bin/ollama`.

## Как повторить (проверенный цикл)
```bash
cd "/home/voron/Документы/VB-ollama/GitHub ollama and others/ollama 0.33.0-rc3"

# 1. Закоммитить работу (чтобы не потерять при rebase/pull)
git add -A && git commit -m "voron: <описание>"

# 2. Rebase на актуальный upstream
git fetch origin main
git rebase origin/main        # при конфликте: править, git add, git rebase --continue

# 3. Проверка
git log --oneline -3          # наш коммит должен быть сверху
go test ./agent/tools/ -count=1
go vet ./cmd/tui/...

# 4. Пересборка + установка (mv, не cp!)
go build -ldflags "-X github.com/ollama/ollama/version.Version=0.33.2-rc0-voron" -o /tmp/ollama-patched .
sudo mv /tmp/ollama-patched /usr/local/bin/ollama && sudo chmod 755 /usr/local/bin/ollama
```

## Состояние после работы
- Репо: `main` = `60b3cc8f` (наш коммит) на `39f7f915` (v0.33.2-rc0).
- `/usr/local/bin/ollama` = 0.33.2-rc0-voron; бэкап старого: `ollama-2026-08-27_19-13`.
- Сервер (`ollama serve`) продолжает работать со старым процессом до перезапуска — предупреждение «client version ...» нормально, исчезнет после рестарта.
- Папка `ollama v0.33.1` рядом — это чистый clone upstream (main @ v0.33.2-rc0), не тронута.

## Примечания
- Кириллические пути в bash всегда в кавычках: `"GitHub ollama and others"`, `ollama 0.33.0-rc3`.
- Перед любым merge/pull — обязательно коммит dirty-файлов (наш урок: работа «висела» в рабочем дереве).
- Свой коммит можно смотреть/откатывать: `git show 60b3cc8f`, откат без потери = `git reset --hard 39f7f915` + коммит сохранится в reflog.
