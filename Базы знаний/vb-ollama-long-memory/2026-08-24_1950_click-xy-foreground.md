# click_xy.py — ТОЛЬКО в ПЕРЕДНЕМ ПЛАНЕ (не через bg-run.sh)

Дата/время: 2026-08-24 ~19:45.

## Правило
`click_xy.py` (pyautogui-клики по координатам) запускать **НЕ В ФОНЕ**:
```bash
cd /home/voron/Документы/VB-ollama/Скрипты_ИИ && python3 click_xy.py <X> <Y> [задержка]
```
Не через `bg-run.sh` — клик не доходит до окна (процесс завершается, но окно не открывается).

## Факты
- Скрипт рабочий: пользователь подтвердил, что интерактивный запуск срабатывает.
- Через `bg-run.sh` (setsid + nohup, логи в /dev/null) клик НЕ доходит.
- Причина не установлена (подозрение: отрыв сессии/seat у pyautogui под setsid). Не тратить время — просто не гонять pyautogui в фоне.

## Исключение
`bg-run.sh` годится для bash-скриптов (зрение.sh, win_*.sh, press_*.sh) — они в фоне работают. Только GUI-клики через python/pyautogui — в переднем плане.
