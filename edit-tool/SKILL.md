---
name: edit-tool
description: "Правильное использование инструмента edit (точечные правки файлов). Загружай перед вызовом edit, если не знаёшь правил: edit принимает ТОЛЬКО относительные пути от cwd — абсолютные пути и выход за cwd вызывают ошибки. Экономит время: сразу правильный вызов без retries."
---

# Инструмент edit — правила использования

## Главное правило: путь
`path` в edit — **только относительный путь от текущего working directory (cwd)**, например `/home/voron`.

| Что передать | Результат |
|---|---|
| Абсолютный путь (`/tmp/a/t.txt`) | ❌ `Error: absolute paths are not allowed` |
| Путь, уходящий за cwd (`../tmp/a/t.txt`) | ❌ `Error: path escapes working directory` |
| Файл в cwd, относительный путь (`edit-test/t.txt`) | ✅ работает |
| Несуществующий файл | ❌ `no such file or directory` — edit НЕ создаёт файлы (только правит существующие) |

## Как получить относительный путь
1. Файл внутри cwd: просто отбрось префикс cwd. `/home/voron/Документы/VB-ollama/state.md` → `Документы/VB-ollama/state.md`.
2. Файл **вне** cwd (например `/tmp`, `/etc`):
   - быстрый путь — править через bash (`sed -i`, `cat > file` и т.п.); для точечных правок edit не обязателен;
   - если нужен именно edit: скопируй файл в cwd, поправь, верни обратно (`cp`).

**Важно (реальный опыт 22.08):**
- «Отбрось префикс cwd» работает ТОЛЬКО когда файл **внутри** cwd. Если файл в **соседнем/другом** дереве (напр. cwd = `/home/voron/Загрузки`, а цель — `/home/voron/Документы/...`), то и путь вида `../Документы/...` уходит выше cwd → ❌ `path escapes working directory`. Не трать ретраи: сразу бери bash (python/sed).
- Инструмент **read** имеет то же ограничение, что и edit: тоже не выходит за cwd. Файлы вне cwd читай через `bash`: `cat`, `sed -n`, `head`.

## Правила old_text / new_text
- `old_text` должен **точно** совпасть с текстом файла (пробелы, переносы, регистр). Не найдено → `Error: old_text was not found`.
- Совпадение должно быть **уникальным**: если строка повторяется — `Error: old_text matched N times; set replace_all to true`.
- Повторяющееся + заменить все вхождения → тот же edit с `"replace_all": true` (принимает только один edit).
- Несколько разных правок в одном файле → один вызов, массив из N edits. Каждый edit матчится против **ОРИГИНАЛА** файла, не против результата предыдущего. Edits не должны пересекаться; объединяй правки на соседних строках в один edit (включи весь блок).

## Мини-процедура
1. `bash`: убедись, что файл существует и увидишь точный текст для old_text (`cat -n` / `sed -n`).
2. Вызови edit с относительным путём от cwd.
3. При ошибке «not found» — перечитай файл и сверь whitespace; при «matched N times» — либо расширь old_text (включи соседние строки для уникальности), либо `replace_all: true`.

## Self-test (проверка, что ты применил скилл)
Успешный вызов edit вернёт `Updated <path> (N edits, M replacements)`. Если получил ошибку про пути — ты нарушил правило №1, см. таблицу выше.
